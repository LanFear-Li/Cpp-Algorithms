#include <stdio.h>

const int MAXN = 1e5;

int main() {
//    int arr[MAXN], n;
//    p_seq_string t = create_null_string_seq(100);
//    p_seq_string p = create_null_string_seq(100);
//
//    char a[] = "abcd";
//    char b[] = "abc";
//    for (int i = 0; i < sizeof a - 1; ++i) {
//        t->c[i] = a[i];
//    }
//    t->n = sizeof a - 1;
//
//    for (int i = 0; i < sizeof b - 1; ++i) {
//        p->c[i] = b[i];
//    }
//    p->n = sizeof b - 1;
//
//    int ans = kmp_match(t, p);
//    printf("%d", ans);

//    p_seq_string str = sub_string_seq(t, 2, 4);
//    for (int i = 0; i < str->n; ++i) {
//        printf("%c", str->c[i]);
//    }

//    p_binary_tree tree = create_null_tree();
//    for (int i = 0; i < n; ++i) {
//        insert(tree, arr[i]);
//    }
//    printf("inorder tree walk as below.\n");
//    inorder_tree_walk(*tree);
//    printf("tree height is: %d\n", get_tree_height(*tree, 0));

//    p_thread_linked_tree tree = create_null_tree_thread();
//    for (int i = 0; i < n; ++i) {
//        insert_thread(tree, arr[i]);
//    }
//    printf("inorder inorder_make_thread tree walk as below.\n");
//    inorder_tree_walk_thread(*tree);
//    inorder_make_thread(*tree);
//    p_thread_linked_node test = find_post_in_pre_order((*tree)->lLink->lLink);
//    printf("this linked_node is: %d", test->info);

//    sort_object *object = generate_random_object(10);
//    show_object(object);
//    quick_swap_sort(object, 0, 9);
//    show_object(object);
    return 0;
}
